### 第一性原则
+ 仅可以新增/更新/删除integration（../..）同目录及其子目录下的文件和文件夹

### 技术规范
+ 严格遵守整体设计文档：../../../DESIGN.md
+ 本模块设计文档：../../DESIGN.md

### 迭代要求
+ Integration介绍：../../REQUIREMENT.md
+ Integration手册：../../USER_GUIDE.md
+ 不能破坏现有设计和功能

### 同步代码
+ ../../../integration/REQUIREMENT.md
+ 所以设计/编译都需要遵守integration的二进制和CLI收口原则

### 需求介绍
+ 为居中输入框 `@` 菜单的可用 Skill 与 SWARM Agent 提供独立缓存，避免每次打开菜单都扫描全部 Agent 的技能、插件和动态元数据。
+ 主应用 `config/config.json` 必须新增以下配置，两个值均为大于 `0` 的整数，单位为秒：
```json
{
  "at": {
    "refresh": 60,
    "cache": 120
  }
}
```
+ `at.refresh` 表示 Integration 主动刷新 `@` 数据缓存的周期；`at.cache` 表示缓存从最近一次成功加载起的有效时长。
+ `at`、`refresh`、`cache` 缺失、为 `0`、负数、非整数或非对象时，Integration 启动必须失败，并输出包含配置键名的明确错误；不得静默回退默认值。
+ 缓存保存所有 Agent 的可用 Skill 与 SWARM 可见性基础数据。首次读取某个 `chatId` 时须按该会话的禁用 Skill 状态创建筛选快照；`POST /api/skill_state` 成功切换技能目录后，必须使用接口返回的禁用路径立即更新该会话筛选快照，不得等待缓存到期或重新扫描 Agent 目录。
+ 每次技能目录禁用或恢复成功后，均须以操作完成时刻重新计算 `at.cache` 到期时间；该更新只影响对应 `chatId` 的 Skill 筛选结果，不得移除其它会话可用的 Skill。
+ 任意 Agent 的 `router_disable`（蜂群开关）写入成功后，必须立即更新基础快照及全部已创建的会话筛选快照中的该 Agent 可见性，并从操作完成时刻重新计算 `at.cache`；`/api/swarm_agent` 不得继续返回切换前的 Agent。
+ Integration 启动后应立即尝试预热缓存，随后每隔 `at.refresh` 秒刷新。刷新成功后必须替换缓存内容并重新计算 `at.cache` 到期时间。
+ 主动刷新失败时必须保留旧缓存，且不得续期或清空缓存；缓存仅在最近一次成功加载后的 `at.cache` 到期时失效。
+ 缓存失效后，`/api/skills` 或 `/api/swarm_agent` 的用户请求必须同步主动加载一次；加载成功后更新缓存与到期时间，加载失败则返回接口错误，不得返回已过期缓存。
+ 同一时刻的主动刷新和用户触发加载必须合并为一次实际扫描，避免并发重复遍历文件系统。
+ 本需求仅优化 `@` 菜单的 `/api/skills` 与 `/api/swarm_agent` 数据源；普通聊天、CLI 心跳、定时任务及其它 Agent metadata 请求仍保持现有实时技能读取语义。

### 编写代码
+ 以 Golang 实现 MCP 配置读取、路径校验、JSON 对象/数组内容判定、按 Agent 刷新状态和元数据注入；优先复用已有配置读取、Agent 元数据、页面告警和请求元数据合并机制，不增加依赖。
+ 刷新状态须以配置的间隔为准，并按 Agent ID 保存有效 MCP 文件的系统绝对路径与解析错误状态。告警接口和页面仍只使用相对路径，避免向页面暴露本机绝对路径。首次请求立即检查；配置的文件名或间隔变化后，下一次检查应使用新配置。
+ 在 `../../main.go` 的普通聊天转发、`/cli/get` 心跳及定时任务直连上游的请求构造处使用同一个注入函数。定时任务覆盖 `cron`、`feishu` 和 `mail` 类型，确保备忘录与两个插件不依赖普通聊天处理器。
+ 新增独立的 MCP 页面告警接口与状态；页面可沿用 `SKILL.md` 解析错误的红色文件标记、错误浮层和清理交互，但不得读取、写入或调用 `/skills_warning`。告警按 Agent 隔离；仅允许在上游请求的 `metadata.mcp` 传递 MCP 文件的系统绝对路径，不得将 MCP 内容、绝对路径或原始解析错误回显到日志或页面。
+ 覆盖自动化测试：配置缺失或非法、相对路径校验、首次与间隔刷新、每个 Agent 独立结果、有效非空对象/数组、空对象/数组、空文件、缺失文件、语法错误、JSON 标量、错误提示的产生与清除、客户端伪造字段剥离，以及普通聊天、`/cli/get`、备忘录、飞书和邮件任务的请求元数据。
+ 作为其他模块可以调用的子模块和可独立运行的CLI命令来编写
+ 在 `main.go` 中以不新增依赖的并发安全缓存状态实现配置校验、预热、周期刷新、请求兜底加载和失效判定；HTTP Handler 仅消费缓存快照，不得在缓存有效期内重复扫描 Agent 目录。
+ `/api/skills` 从缓存快照定位 Agent，并复用对应 `chatId` 的筛选快照；`/api/swarm_agent` 从同一基础快照筛选 `router_disable=false` 并排除当前 Agent。
+ 覆盖自动化测试：配置缺失、`at` 非对象、`refresh/cache` 为零/负数/非整数、首次预热、定时刷新成功续期、定时刷新失败保留旧缓存且不续期、到期请求成功更新、到期请求失败返回错误、并发请求合并、Skill chat 级过滤、技能禁用/恢复后的缓存即时筛选与续期、蜂群开关写入后的全缓存即时更新与续期，以及 Agent 筛选。
+ 最小范围更新

### 撰写手册
+ 编写USER_GUIDE.md

### 其他要求
+ REQUIREMENT.md为需求文档，禁止编写
