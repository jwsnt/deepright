### 第一性原则

+ 仅可以新增/更新/删除site（../..）同目录及其子目录下的文件和文件夹。

### 技术规范
+ 严格遵守整体设计文档：../../../DESIGN.md
+ 本模块设计文档：../../DESIGN.md
+ 不新增第三方依赖，不破坏既有虚拟文件系统、文件预览或媒体编辑功能。

### 需求介绍
+ `__internal_miniapp` 仅可将 `#workspace/app/API.md`、`#workspace/app/CANVAS.md`、`#workspace/app/DESIGN.md` 作为只读参考资料：不得新增、修改、覆盖、移动或删除这三个文件。
+ 迷你应用调用 Integration API 时优先使用当前页面同源地址；不得从上述文档中的示例端口推导或硬编码服务端口。
+ `miniapp.recover` 是服务端维护配置，Site 保持对 `miniapp` 完整受控透传的兼容，不将恢复状态写入 Agent 工作目录或浏览器持久化状态。

### 编写代码
+ 以现有 site 页面技术栈在 `../../index.html` 最小范围实现；不引入新的构建流程、第三方依赖或持久化状态。

### 撰写手册
+ 更新 `../../USER_GUIDE.md` 及本迭代目录 `USER_GUIDE.md`，说明 `miniapp` 配置格式、图标位置、CLI／Git 地址输入、功能列表的 `+`／`−` 操作、全角顿号拼接、默认功能回退与确认后的自动发送行为，以及三份只读参考文档和同源 API 约定。

### 其他要求
+ `REQUIREMENT.md` 仅描述需求，不记录实现过程。
