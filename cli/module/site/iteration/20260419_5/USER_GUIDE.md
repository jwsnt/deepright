# Site 迭代 20260419_5 使用手册

## 变更说明

设置面板中「选择 Agent」下拉框右侧新增 30×30px 文件夹图标（默认透明度 30%），点击后弹出二次确认浮层，确认后调用 `/api/folder?agentId=xxx` 打开对应 Agent 的 workspace 目录。

## 使用方式

1. 打开设置，选择一个 Agent
2. 点击下拉框右侧的目录图标
3. 在确认浮层中点击「打开」
4. 系统文件管理器将打开该 Agent 的工作目录
