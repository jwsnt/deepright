本次迭代为设置里的模型客户化配置补充了悬停全文预览。

- 适用范围：`基础模型`、`快速响应`、`深度思考`、`多模态输入`、`多模态输出`
- 触发条件：输入框内已有内容，且内容宽度超过当前输入框可见宽度
- 交互效果：鼠标悬停在对应输入框上时，会在输入框下方展示一个悬浮预览，完整显示当前输入值
- 宽度规则：悬浮预览宽度按实际文字宽度计算，不再沿用输入框自身宽度
- 消失时机：鼠标移开、设置浮层关闭、切换模型条目、滚动或窗口尺寸变化时，悬浮预览都会立即关闭
