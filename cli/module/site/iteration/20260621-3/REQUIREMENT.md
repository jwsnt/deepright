### 第一性原则
+ 仅可以新增/更新/删除site（../..）同目录及其子目录下的文件和文件夹

### 技术规范
+ 严格遵守整体设计文档：../../../DESIGN.md
+ 本模块设计文档：../../DESIGN.md

### 迭代要求
+ Site介绍：../../REQUIREMENT.md
+ Site手册：../../USER_GUIDE.md
+ 不能破坏现有设计和功能

### 同步代码
+ ../../REQUIREMENT.md
+ 所以设计/编译都需要遵守site页面的现有前端收口原则

### 需求介绍
+ 在待发送消息列表每个待发送消息的编辑按钮前增加插入小图标，点击后调用/api/message_insert/add为该消息插入上报，mid为当前时间戳
    + Integration接口需求：../../../integration/iteration/20260621-1/REQUIREMENT.md
    + 待发送消息列表需求：../20260619-2/REQUIREMENT.md
+ 待发送消息列表每个待发送消息继续保持3个操作按钮：插入、编辑、删除（从左到右），并保留现有拖动手柄：
    + 插入按钮用于将当前待发送消息加入插入上报；进入插入态后按钮切换为进行中小图标，并按本需求继续轮询状态
    + 编辑按钮在未进入插入态时保持现有编辑行为；已进入插入态时先调用/api/message_insert/del撤销插入态，再进入现有编辑流程
    + 删除按钮在未进入插入态时保持现有本地删除行为；已进入插入态时改为调用/api/message_insert/del取消加入插入消息，仅清除插入态而不删除本地消息
+ 插入成功后该待发送消息持续高亮，插入小图标切换为进行中小图标，并每5秒按当前ChatId批量调用一次/api/message_insert/status轮询状态：
    + 单个待发送消息作用域内最多同时跟踪5条插入中的消息
    + 页面上的插入状态、按钮状态和动画效果需要与当前Agent + Chat作用域绑定，切换会话或切换Agent时不要相互影响
    + 如果状态为取消、已上传，则将待发送消息从列表中移除，并展示明显的爆炸移除动画
    + 如果状态轮询出现任何HTTP异常并连续超过2次，则该待发送消息切换为红色高亮，但不自动移除，后续继续按现有轮询节奏自动重试
+ 在轮询/api/message_insert/status得知消息成功上报（插入后）时，需要立即为被插入消息生成一个SSE请求气泡：
    + 气泡样式与普通用户发送消息一致
    + 气泡位置在发起这次最初请求的SSE气泡之下、等待SSE响应的气泡之上；若当前尚未出现等待中的SSE响应气泡，则先追加到当前会话消息末尾
    + 该气泡首次出现时需要有明显的插入入场动画，并且动画只在首次插入到当前会话时播放一次，不能因后续重渲染、切换会话或恢复渲染而反复播放
+ 已进入插入态的待发送消息再次点击编辑时，需要先调用/api/message_insert/del撤销当前插入态，再进入现有编辑流程：
    + 编辑保存后仅更新本地待发送消息内容，不自动重新进入插入态；如需再次插入，由用户重新点击插入小图标触发
+ 已进入插入态的待发送消息点击删除按钮时，不再直接删除本地待发送消息，而是调用/api/message_insert/del取消加入插入消息：
    + 取消成功后仅清除插入态、高亮态和轮询跟踪，待发送消息继续保留在本地待发送列表中
+ 如果待发送消息队列轮到该消息时该消息尚未被上游消费，则在自动发送前best-effort调用/api/message_insert/del撤销插入上报：
    + 无论接口返回是否异常，都继续自动发送，不因删除接口异常阻塞发送

### 编写代码
+ 以现有site页面技术栈编写以上代码，要求：
    + 在../../index.html中按现有HTML/CSS/JavaScript组织方式实现
    + 不引入新的构建流程和额外运行时依赖
    + 代码简洁，尽量复用现有待发送消息队列、消息编辑、提示动效和轮询状态同步逻辑
    + 能用现有开源包和浏览器能力的就不要重复造轮子
+ 最小范围更新

### 撰写手册
+ 如有必要同步更新USER_GUIDE.md

### 其他要求
+ REQUIREMENT.md为需求文档，禁止编写
