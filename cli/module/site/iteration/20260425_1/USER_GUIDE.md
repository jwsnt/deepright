# Site 迭代 20260425_1 使用手册

## 变更说明

新增 Agent 删除后的会话检查机制。

## 功能说明

- 删除 Agent 后，若当前会话使用了该 Agent，自动弹出 Agent 选择浮层
- 切换会话时，若目标会话绑定的 Agent 已不存在，自动弹出选择浮层
- 发送消息前检查 Agent 有效性，无效时阻止发送并弹出选择浮层
- 浮层未选择时切换会话，浮层自动收起，切换后重新检查新会话的 Agent
- 浮层显示期间点击外部区域不会关闭，必须选择 Agent 后才能继续操作
- 选择 Agent 后自动绑定到当前会话并关闭浮层
