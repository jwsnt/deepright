# Site 迭代手册（20260726-7）

## `canvas` 目录新手引导

第一次在当前 Agent 的虚拟文件系统中悬停或点击 `canvas` 目录时，会显示独立的新手引导。

- `canvas` 目录会被高亮，其它区域会被暗化并暂时不可点击。
- 引导说明为“canvas目录用于保存裁剪后音频。”
- 点击 `完成` 后关闭遮罩，页面恢复正常操作；随后再次点击目录即可按原方式进入。
- 完成记录仅保存于当前浏览器，与首次使用、`audios`、`videos` 及其它引导记录相互独立。
