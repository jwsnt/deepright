# Site 迭代 20260425_2 使用手册

## 变更说明

设置面板中 Agent 下拉框旁新增小蜜蜂图标，用于配置蜂群（swarm）。

## 功能说明

### 设置面板
- Agent 下拉框右侧显示小蜜蜂图标（有 Agent 时显示，无 Agent 时隐藏）
- 蜂群开启时图标彩色（金色），关闭时灰色
- 点击图标展开蜂群配置面板：开关 + 描述（最多 200 字）
- 保存后调用 `/api/config` 更新 config.json

### 输入区域
- 蜂群开启时，小太阳图标后追加小蜜蜂图标，同节奏闪烁
- 蜂群关闭时小蜜蜂图标消失
