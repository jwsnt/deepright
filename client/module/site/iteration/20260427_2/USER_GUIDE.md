# 非本地访问拖入限制

## 变更说明

非本地访问时(host 不是 localhost 或 127.0.0.1), 禁止从系统文件管理器拖入文件或目录到居中对话框和右上备忘录。

## 视觉提示

- 禁止拖入时, 输入框光圈变为红色闪烁
- 同时显示红色提示: "远程访问不支持拖入系统文件"
- 鼠标指针变为禁止图标(dropEffect: none)

## 允许的操作

- 从虚拟文件系统(VFS)拖入: 始终允许, 光圈为蓝色

## 禁止的操作(非本地访问时)

- 从系统文件管理器拖入文件或目录
- 拖入纯文本路径
