# Site 迭代 20260421_3 使用手册

## 变更说明

设置面板中"打开 Agent 目录"右侧新增 `+` 图标，点击后弹出名称输入框，创建新 Agent 并刷新下拉列表。

## 使用方式

1. 打开设置，点击 Agent 下拉框右侧的 `+` 图标
2. 输入 Agent 名称（仅英文字母、数字、连字符、下划线，无空格）
3. 点击「创建」，调用 `/api/agent/init` 创建目录
4. 创建成功后自动刷新 Agent 下拉列表并选中新 Agent
5. 点击「取消」关闭输入框
