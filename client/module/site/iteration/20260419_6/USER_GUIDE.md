# Site 迭代 20260419_6 使用手册

## 变更说明

对话框发送按钮右侧（输入框外）新增 15×15px 文件夹图标（默认透明度 50%），点击后弹出二次确认浮层，确认后调用 `/api/folder?agentId=xxx` 打开当前已选 Agent 的 workspace 目录。

## 使用方式

1. 确保已在设置中选择了 Agent
2. 点击发送按钮右侧的文件夹图标
3. 在确认浮层中点击「打开」
4. 系统文件管理器将打开该 Agent 的工作目录

## 注意事项

- 未选择 Agent 时点击会提示先在设置中选择
- 设置面板中的文件夹图标功能不受影响
