# Site 迭代 20260422_3 使用手册

## 变更说明

Agent 和模型绑定会话，每个会话独立。

## 行为

- 设置中选择 Agent 后绑定到当前会话
- 选择模型后绑定到当前会话
- 输入框上方 25px 悬浮框居中显示当前 Agent 和模型（如 `a · kimi`）
- 切换会话时自动恢复该会话的 Agent 和模型，刷新 VFS
- 发送消息时使用当前会话绑定的 Agent
- 删除会话时清理绑定
