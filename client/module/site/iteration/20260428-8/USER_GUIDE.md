# 迭代 20260428-8：备忘录列表固定高度滚动与自动定位

## 功能说明

1. 备忘录明细列表和元数据列表均为固定高度，数据过多时在容器内滚动
2. 明细列表加载后自动滚动到下一个待执行任务的位置

## 技术实现

- Grid 容器和子元素设置 `min-height:0`，确保 `overflow-y:auto` 生效
- 渲染明细列表时标记第一个 `started=0 && execTime > now` 的条目，渲染完成后 `scrollIntoView` 平滑滚动到该位置
