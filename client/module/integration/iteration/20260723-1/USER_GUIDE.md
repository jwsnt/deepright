# 迭代 20260723-1：Agent 切换时的备忘录请求隔离

切换 Agent 后，页面继续使用既有备忘录任务明细和元数据接口按 Agent 标识刷新内容；本次不新增 HTTP 接口、数据库字段或服务端持久化状态。

## 请求行为

- 页面连续切换 Agent 时，会取消仍在等待的旧请求；服务端只需按既有请求上下文结束该次连接。
- 即使旧请求已经完成，页面也会忽略其响应，仅接受最新 Agent 对应的结果。
- 取消或完成这些读取请求不会写入、删除或修改 Agent、备忘录、聊天、记忆、技能、通知或其他运行数据。
