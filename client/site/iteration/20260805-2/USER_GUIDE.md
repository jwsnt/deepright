# 会话沙盒的 Bubblewrap 预检

在 WSL/Linux 上，完成沙盒新手引导后点击会话沙盒按钮，页面会先检查 Bubblewrap（`bwrap`）是否可用。检查完成前界面会短暂锁定，避免重复操作。

如果依赖可用，页面照常打开会话沙盒浮层；如果缺少依赖，浮层不会打开，而会询问是否发送安装请求。选择“安装”或按 `Enter` 后，系统会把服务端配置的安装请求自动发送到当前 Chat；取消、点击遮罩或按 `Esc` 不会发送任何内容。页面不会自行执行安装命令。

macOS 不需要这项预检，现有会话沙盒交互保持不变。
