# SSE 响应打字机效果与 CLI 子任务面板

## 打字机效果

居中会话框展示 SSE 响应时，助手消息末尾显示闪烁光标（▌），流结束后消失。

## CLI 子任务面板

- SSE 响应中 `biz=cli && workflow=sub` 的内容重定向到右侧边栏第二排（合并为一整行）
- 气泡字体 50%，高度 50%，两侧 padding 10px
- 内容与 Agent+Chat 绑定，切换会话或刷新后恢复
- 独立存储在 `cliSubContent` 字段，不混入居中对话框
