# Site 迭代 20260426_1 使用手册

## 变更说明

在输入框发送按钮正上方新增 Thinking 滑动按钮，与设置面板中的 Thinking 开关双向同步。

## 功能说明

- 发送按钮上方显示小型 Thinking 滑动开关和状态标签（Thinking/Auto）
- 有 Agent 绑定时显示，无 Agent 时隐藏
- 切换后立即保存到 config.json，与设置面板中的 Thinking 开关状态同步
- 每个 Agent 的 Thinking 配置独立，切换会话时自动更新
