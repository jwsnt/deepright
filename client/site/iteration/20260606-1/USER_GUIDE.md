# Site 迭代 20260606-1 使用手册

## 一次性备忘录创建时间

- 当右上角备忘录周期选择为 `仅一次` 时，点击创建后站点会忽略当前日历和时间选择器里的值，改为按实际保存当刻的当前时间提交。
- 这个“当前时间”按分钟精度写入 `rawTime`，从而避免因为所选时间早于当前时间而收到 `一次性任务执行时间不能早于当前时间` 的失败提示。
- 周期任务（工作日、自然日、每小时、每15分钟、每30分钟）仍继续使用界面上选择的日期和时间，不受这次调整影响。
