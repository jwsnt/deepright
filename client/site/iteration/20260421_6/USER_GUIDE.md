# Site 迭代 20260421_6 使用手册

## 变更说明

虚拟文件系统头部返回上级图标右侧新增"新建"和"打开"按钮。

## 新建
- 点击 `+` 图标弹出浮层
- 输入名称，选择文件或目录
- 调用 `/api/agent/create` 创建，成功后刷新 VFS

## 打开
- 点击文件夹图标
- 调用 `/api/folder?agentId=xxx&dir=yyy` 打开当前 VFS 浏览目录
