{
  "main": {
    "assistant": "def-skills",
    "funCall": {
      "description": "The content of the specified skill (Skills) resource shall be retrieved.",
      "properties": {
        "why_do_this": {
          "type": "string",
          "description": "Brief reasoning for executing this command. Must retain granular data and operational logs for process summarization. The more granular, the better."
        },
        "resource": {
          "type": "string",
          "description": "The resource path of the skill (Skills) (e.g. `SKILL.md` for the main skill content, or a relative path indicated in the skill content for other assets)."
        },
        "skill": {
          "type": "string",
          "description": "The name (name) of the skill (Skills)."
        }
      },
      "required": [
        "why_do_this",
        "resource",
        "skill"
      ]
    }
  }
}
